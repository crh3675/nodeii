/*
 * Create configuration overrides for each environment.  
 * We created this basic `local.js` configuration but you can 
 * have one for Development, Production, UAT or any 
 * other environnment you need
 */
var extend = require('extend'),
   defaults = require('../default');  

module.exports = extend(true,  defaults, {

});